<!--
模板：Skill 封装（复杂、可复用、按需披露的专业流程）。
用法：在目标项目 skills/<skill-name>/ 下创建 SKILL.md。需要脚本/资源就放同目录。
判断是否该用 Skill 而非 Command：见 harness-over-harness/GUIDE/orchestration.md 第 3 节。
description 决定 agent 能否"自动发现"该 skill，务必写清触发场景与关键词。
-->
---
name: {{skill-name}}
description: {{一段话：这个 skill 做什么、什么场景/关键词下应被触发、能力边界。写得越精准，agent 越能在正确时机自动加载它。}}
---

# Skill: {{Skill 名称}}

## 何时使用（触发条件）
- 当 {{场景 A}} 时。
- 当用户提到 {{关键词 1}} / {{关键词 2}} 时。

## 何时**不**使用（边界）
- {{不适用的场景，避免误触发}}

## 前置条件
- {{需要的环境 / 凭据(只走环境变量) / 输入}}

## 流程（SOP）
1. {{第一步}}
2. {{第二步}}
3. {{第三步：闭环自检——对照原始需求验证结果}}

## 可执行资源（可选）
- `./scripts/{{script}}` — {{用途；遵守红线：少用 shell、避免 RCE、密钥走环境变量}}

## 输出
- {{交付物形态与验收标准}}
